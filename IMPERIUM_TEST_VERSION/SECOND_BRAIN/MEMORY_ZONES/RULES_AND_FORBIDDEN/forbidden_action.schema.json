{
  "schema_id": "forbidden_action_v0.2",
  "title": "ForbiddenAction",
  "description": "Schema for a forbidden action rule",
  "type": "object",
  "required": ["id", "action", "reason", "severity"],
  "properties": {
    "id": {"type": "string"},
    "action": {"type": "string"},
    "reason": {"type": "string"},
    "severity": {"type": "string", "enum": ["CRITICAL", "HIGH", "MEDIUM"]},
    "auto_fail": {"type": "boolean"},
    "applies_to": {"type": "array", "items": {"type": "string"}},
    "exceptions": {"type": "array", "items": {"type": "string"}}
  }
}
