# NEXT FIX CANDIDATES

- no continuous full redraw;
- draw static map once;
- use tags/layers;
- separate tooltip layer;
- on hover update only tooltip, not whole map;
- on resize redraw once with debounce;
- no animation until stable baseline;
- consider pre-rendering background/planet to image buffer later;
- consider Canvas item coords updates instead of delete/redraw.
