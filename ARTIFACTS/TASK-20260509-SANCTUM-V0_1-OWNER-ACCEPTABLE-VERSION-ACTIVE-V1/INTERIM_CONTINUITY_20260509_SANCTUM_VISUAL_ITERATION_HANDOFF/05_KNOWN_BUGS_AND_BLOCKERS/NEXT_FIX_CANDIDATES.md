# NEXT FIX CANDIDATES

- remove continuous redraw completely;
- avoid `canvas.delete("all")` on hover;
- draw static layer once;
- use tags to update only tooltip layer;
- use separate overlay canvas for tooltip;
- redraw only on task selection/resize;
- consider Pillow-backed offscreen image buffer later if needed;
- avoid animation until stable visual baseline is accepted.
