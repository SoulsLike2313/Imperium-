# VM2 ROUTE SOURCE NOTE

Route profile source:
- Local SSH library route context and local VM2 probe receipt.

Security handling:
- Raw secrets are not copied into task artifacts.
- Route values are used for execution only.

Usage scope:
- PC to VM2 route is used only for worker-room bootstrap in this task.
