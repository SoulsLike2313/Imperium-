{
  "schema_version": "imperium.stage_map.v0_1",
  "stage_map_id": "STAGE-MAP-DRAFT-20260513-INQUISITION-V0_1-SELF-BUILD-V0_1",
  "task_id": "TASK-CANDIDATE-20260513-BUILD-INQUISITION-V0_1-SELF-DESCRIPTIVE-ORGAN",
  "status": "DRAFT_NEEDS_REVIEW",
  "stages": [
    {
      "stage_id": "STAGE-001-REGISTER-INQUISITION-BLUEPRINT",
      "goal": "Register Inquisition blueprint contracts and references.",
      "inputs": ["task_candidate", "act3 registries"],
      "outputs": ["inquisition blueprint draft registry"],
      "allowed_paths": ["ORGANS/INQUISITION", "schemas", "DOCS"],
      "forbidden_paths": ["THRONE", ".git"],
      "required_scripts": ["SCRIPT-CHECK-ACT4-REGISTRATION-CORRIDOR-V0_1"],
      "required_capabilities": ["organ_contract_validation"],
      "checks": ["schema json validation", "script registry check"],
      "receipts": ["bundle receipt", "check report"],
      "owner_decision_gates": ["blueprint accepted for review export"],
      "pass_criteria": ["blueprint draft exists", "no blockers in checks"],
      "fail_criteria": ["missing schema references", "unsafe scope drift"],
      "bundle_policy": "VM2 bundle only"
    },
    {
      "stage_id": "STAGE-002-EXPORT-REVIEW-PACK",
      "goal": "Export technical review pack for external/internal reviewer.",
      "inputs": ["blueprint draft", "review questions"],
      "outputs": ["review pack json/md"],
      "allowed_paths": ["ORGANS/ASTRONOMICON/REGISTRY/REVIEW_PACKS", "DOCS"],
      "forbidden_paths": ["THRONE", ".git"],
      "required_scripts": [],
      "required_capabilities": ["technical_review_export"],
      "checks": ["review pack structural check"],
      "receipts": ["review pack export receipt"],
      "owner_decision_gates": ["questions approved"],
      "pass_criteria": ["pack ready and evidence-backed"],
      "fail_criteria": ["missing constraints/no-fake-green rules"],
      "bundle_policy": "VM2 bundle only"
    },
    {
      "stage_id": "STAGE-003-INGEST-ADVISORY-RESPONSES",
      "goal": "Ingest structured advisory responses into registry.",
      "inputs": ["advisory responses"],
      "outputs": ["advisory response records"],
      "allowed_paths": ["ORGANS/ASTRONOMICON/REGISTRY/ADVISORY_RESPONSES"],
      "forbidden_paths": ["THRONE", ".git"],
      "required_scripts": [],
      "required_capabilities": ["advisory_response_ingest"],
      "checks": ["advisory_response schema check"],
      "receipts": ["ingest receipt"],
      "owner_decision_gates": ["raw vs accepted items decision"],
      "pass_criteria": ["responses ingested with status"],
      "fail_criteria": ["raw advisory treated as doctrine"],
      "bundle_policy": "VM2 bundle only"
    },
    {
      "stage_id": "STAGE-004-MODERNIZE-TASK-AND-STAGE-MAP",
      "goal": "Apply accepted recommendations and update candidate+stage map.",
      "inputs": ["advisory response records"],
      "outputs": ["task modernization record", "updated candidate", "updated stage map"],
      "allowed_paths": ["ORGANS/ASTRONOMICON/REGISTRY/TASK_MODERNIZATIONS", "ORGANS/ASTRONOMICON/REGISTRY/TASK_CANDIDATES", "ORGANS/ASTRONOMICON/REGISTRY/STAGE_MAPS"],
      "forbidden_paths": ["THRONE", ".git"],
      "required_scripts": [],
      "required_capabilities": ["task_modernization"],
      "checks": ["consistency check"],
      "receipts": ["modernization receipt"],
      "owner_decision_gates": ["accept/reject recommendation decisions recorded"],
      "pass_criteria": ["traceability from recommendation to change"],
      "fail_criteria": ["untraceable changes"],
      "bundle_policy": "VM2 bundle only"
    },
    {
      "stage_id": "STAGE-005-BUILD-INQUISITION-CONTRACT-SCHEMA-REGISTRY",
      "goal": "Create Inquisition v0.1 contract/schema/registry set.",
      "inputs": ["approved modernization"],
      "outputs": ["inquisition contract artifacts"],
      "allowed_paths": ["ORGANS/INQUISITION", "schemas"],
      "forbidden_paths": ["THRONE", ".git"],
      "required_scripts": ["SCRIPT-CHECK-ACT4-REGISTRATION-CORRIDOR-V0_1"],
      "required_capabilities": ["organ_contract_validation"],
      "checks": ["schema + organ checker"],
      "receipts": ["build receipts"],
      "owner_decision_gates": ["contract acceptance"],
      "pass_criteria": ["contract+schema machine-checkable"],
      "fail_criteria": ["missing forbidden_actions or receipts"],
      "bundle_policy": "VM2 bundle only"
    },
    {
      "stage_id": "STAGE-006-BUILD-INQUISITION-SELF-REPORT-PORT",
      "goal": "Implement first self-report port for Inquisition.",
      "inputs": ["inquisition contract"],
      "outputs": ["self-report artifact"],
      "allowed_paths": ["ORGANS/INQUISITION", "schemas"],
      "forbidden_paths": ["THRONE", ".git"],
      "required_scripts": [],
      "required_capabilities": ["organ_self_report"],
      "checks": ["self-report schema check"],
      "receipts": ["self-report receipt"],
      "owner_decision_gates": ["self-report format acceptance"],
      "pass_criteria": ["self-report generated with required fields"],
      "fail_criteria": ["missing stale/unknown/blocker fields"],
      "bundle_policy": "VM2 bundle only"
    },
    {
      "stage_id": "STAGE-007-BUILD-FIRST-INQUISITION-AUDIT-CHECKER",
      "goal": "Create first checker enforcing no-fake-green for Inquisition.",
      "inputs": ["contract + self-report"],
      "outputs": ["checker script", "runtime receipts"],
      "allowed_paths": ["TOOLS", "ORGANS/INQUISITION", "REGISTRY"],
      "forbidden_paths": ["THRONE", ".git"],
      "required_scripts": [],
      "required_capabilities": ["inquisition_audit"],
      "checks": ["py_compile", "checker self-run"],
      "receipts": ["audit checker receipt"],
      "owner_decision_gates": ["audit threshold policy"],
      "pass_criteria": ["checker blocks missing evidence"],
      "fail_criteria": ["checker permits fake green"],
      "bundle_policy": "VM2 bundle only"
    },
    {
      "stage_id": "STAGE-008-VM2-EXECUTION-BUNDLE",
      "goal": "Prepare scoped VM2 bundle with evidence.",
      "inputs": ["stage outputs"],
      "outputs": ["bundle zip + sha256"],
      "allowed_paths": ["_handoff_out"],
      "forbidden_paths": ["THRONE", ".git", "secrets"],
      "required_scripts": ["SCRIPT-VERIFY-WORKER-BUNDLE-V0_1"],
      "required_capabilities": ["bundle_provenance"],
      "checks": ["bundle verifier"],
      "receipts": ["manifest", "receipt", "verdict"],
      "owner_decision_gates": ["bundle accepted for PC intake"],
      "pass_criteria": ["bundle verification PASS/PASS_WITH_WARNINGS"],
      "fail_criteria": ["bundle rejected or missing proof"],
      "bundle_policy": "VM2 bundle only"
    },
    {
      "stage_id": "STAGE-009-PC-INTAKE-REVIEW-COMMIT",
      "goal": "PC performs intake/review and commit decision.",
      "inputs": ["VM2 bundle"],
      "outputs": ["PC review report", "commit decision"],
      "allowed_paths": ["PC contour only"],
      "forbidden_paths": ["VM2 direct commit/push"],
      "required_scripts": ["SCRIPT-REVIEW-WORKER-BUNDLE-INTAKE-PS1-V0_1"],
      "required_capabilities": ["pc_intake_review"],
      "checks": ["pc pre-commit checks"],
      "receipts": ["PC intake report"],
      "owner_decision_gates": ["commit approval"],
      "pass_criteria": ["CAN_COMMIT or NEEDS_OWNER_DECISION with evidence"],
      "fail_criteria": ["BLOCKED/CANNOT_COMMIT unresolved"],
      "bundle_policy": "PC controlled"
    },
    {
      "stage_id": "STAGE-010-VM2-SYNC-AND-SELF-CHECK-RECEIPT",
      "goal": "Sync VM2 from PC commit and emit self-check receipt.",
      "inputs": ["PC commit/push result"],
      "outputs": ["VM2 sync receipt", "git truth receipt"],
      "allowed_paths": [".imperium_runtime"],
      "forbidden_paths": ["VM2 local commit/push"],
      "required_scripts": ["SCRIPT-ADMINISTRATUM-GIT-CLI-CHECK-SH-V0_1"],
      "required_capabilities": ["git_truth_receipts"],
      "checks": ["git cli check"],
      "receipts": ["GIT_CLI_CHECK_RECEIPT"],
      "owner_decision_gates": ["post-sync acceptance"],
      "pass_criteria": ["HEAD/origin/remote match"],
      "fail_criteria": ["stale baseline mismatch"],
      "bundle_policy": "runtime receipt only"
    }
  ]
}
